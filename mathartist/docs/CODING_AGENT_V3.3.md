# Coding agent mission — MathArtist V3.3

Do not redesign the architecture. The bottleneck is data and execution.

Priority order:
1. Fetch and normalize real #つぶやきProcessing records with attribution.
2. Reproduce each default sketch exactly in a sandboxed p5 runtime.
3. Extract numeric/control candidates; classify observer/runtime constants separately from mathematical constants.
4. Require provenance for every admitted mutation control.
5. Run 1k+ blind descendants per seed. Parallelize aggressively.
6. Store genotype, seed hash, parent, RNG seed, runtime version, render hash, technical validity, morphology descriptor.
7. Build blind contact sheets ordered randomly, not by beauty.
8. Human POTENTIAL creates local expansion jobs (small/medium/large sigma); never train OPEN on POTENTIAL.
9. Measure seed fertility as diversity/coverage of viable descendants, not aesthetic score.
10. Add biology/frontier math only after the 25-seed Processing benchmark passes.

Never:
- invent replacement equations when a real seed exists;
- mutate fixed source equations under the same provenance identity;
- use a VLM to score beauty in OPEN;
- conflate pixel distance with morphological novelty;
- silently redistribute third-party code with unknown license;
- claim QRI/rasa/valence validation from adaptive exploratory data.

After benchmark, produce a peer-review report listing failure modes and examples where parameter mutation collapses, explodes, or yields distinct morphological basins.
