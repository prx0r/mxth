# V3.3.0
- Converts the #つぶやきProcessing plan from a link manifest into an executable local corpus-ingestion path.
- Adds attribution/hash-preserving feed normalization.
- Adds conservative numeric control-candidate extraction with observer/runtime separation.
- Adds explicit 25-seed × 1,000-descendant acceptance benchmark.
- Freezes the dumb-high-volume / human-POTENTIAL division as a protocol.
- Adds coding-agent instructions focused on data rather than architecture.

## V3.3.0 build 2 — NS26 data-first completion
- NS26 upgraded from legacy `interpretation_genes` to an evidence-backed `control_surface`: 11 observation-only controls, each with provenance, role and claim scope. DERIVED status kept prominent (dashboard chip + spec + docs).
- Frozen NS26 source bytes: `app/static/data/ns26/base.f32.bin` (8192-point quasi-random scaffold) + `schedule.json` (120-frame tau/rs/zs/vel schedule), generated byte-reproducibly by `tools/generate_trajectories.py::generate_ns26`.
- New frozen mechanism module `app/static/js/sources/ns26_core.js`; `ns26.js` is now a pure observer over frozen bytes. Python/JS scaffold math cross-checked to 1e-6.
- `#つぶやきProcessing` corpus materialized: 994/994 attributed records with code hashes (`corpus/tsubuyaki/local_registry.jsonl`), local-only code cache (excluded from releases by `tools/package.py`), corpus-wide literal mining (`control_candidates.json`: 14,252 literals).
- `docs/benchmarks/v3.3-corpus/` filled with real artifacts: NS26 96-specimen control audit (96/96 valid, 0 duplicates) + 256-specimen discovery sweep (256/256 valid, 72 kept for human POTENTIAL review).
- Worked array-sequence import example: `VORTEX_DECAY_DEMO` (synthetic, honestly labeled) proving the paper/model ingestion path end to end.
- New `tests/test_ns26_source.py` (8 tests incl. render-hash fixture); suite at 25 passed; `scripts/verify_suite.py` green.
- Release hygiene: version strings corrected (package.json + README H1), all `__pycache__`/`.pytest_cache`/WAL sidecars removed, audit header de-versioned.
- Note: regenerating trajectories with the bundled generator reproduces LENIA + center-hyperpolarized + uniform-bistable bytes exactly, but NOT the shipped `french-flag.f32.bin` (release predates current generator for that variant). Shipped bytes restored as the frozen source; recorded in `docs/ARCHIVE_MAP.md`.

## V3.3.0 build 3 — upstreams, zip-gap review, art production
- `external/` materialized: 7 vendor-script repos + 6 HIGH-priority processing/growth/math-art references (p5.js 2.3.2, skepara, SmoothLife, physarum, differential-line, plottter). All 6 UPSTREAMS.lock commits pinned exactly; floating HEADs recorded in `external/FETCHED.md`. Excluded from releases and (new `pytest.ini`) from test collection.
- Zip-gap review: the one genuinely missing piece across all 11 zips was lab-v2 experiment E0 (source visual identity). Implemented as `tools/e0_source_identity.py`: 1-NN LOO accuracy 0.549 vs 0.167 chance — NS26 (0.75) and TINYMAPS (0.71) separate cleanly; BIOELECTRIC/CHLADNI/LENIA/VORTEX overlap in the coarse 6-descriptor space. Recorded as descriptor coarseness, not a blocker. v1 `NS26_CORE` artist cited as `v1_lineage` in the spec (non-hashed field); lab-v2 QD example confirmed superseded by `integrations/pyribs/run_qd.py`.
- Art production running: fresh 384-blind-sample NS26 gallery (`ns26-gallery/`, 100% valid, 72 coverage picks for POTENTIAL review) alongside the earlier audit + discovery artifacts.
